/**
 * Represents the static air background layer.
 * @extends Background
 */
class Air extends Background {

    /** Creates an air layer and loads its background image. */
    constructor() {
        super().loadImage('img/5_background/layers/air.png');
    }
}
